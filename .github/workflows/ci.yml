name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  quality-and-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: Install project
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Lint
        run: ruff check .

      - name: Run tests
        run: pytest -q --cov=it_ticket_priority --cov-report=term-missing

      - name: Prompt and RAG contract quality gate
        run: |
          python scripts/run_prompt_evals.py \
            --output /tmp/prompt_eval_metrics.json \
            --fail-on-regression

      - name: Offline copilot smoke test
        run: python scripts/run_copilot_demo.py > /tmp/copilot_demo.json

      - name: Training smoke test
        run: |
          python scripts/train_model.py \
            --rows 1500 \
            --quick \
            --model-path /tmp/priority_model.joblib \
            --metadata-path /tmp/model_metadata.json \
            --artifact-dir /tmp/artifacts \
            --asset-dir /tmp/assets
